import express from 'express';
import multer from 'multer';

const app = express();
const PORT = Number(process.env.PORT || 10000);
const GEMINI_KEY = process.env.GEMINI_API_KEY || process.env.GOOGLE_API_KEY || '';
const CARHUNT_KEY = process.env.CARHUNT_API_KEY || '';
const MODELS = ['gemini-2.5-flash-lite', 'gemini-2.5-flash'];
const VERSION = '2026-09-12.4';
const upload = multer({ storage: multer.memoryStorage(), limits: { files: 3, fileSize: 12 * 1024 * 1024 } });

app.disable('x-powered-by');
app.use(express.json({ limit: '1mb' }));
app.use((req,res,next)=>{res.setHeader('Cache-Control','no-store, no-cache, must-revalidate');res.setHeader('Pragma','no-cache');next();});

const num=v=>{if(v===null||v===undefined||v==='')return null;const n=Number(String(v).replace(/\s/g,'').replace(',','.'));return Number.isFinite(n)?n:null;};
const norm=v=>String(v??'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim();
const same=(a,b)=>{const x=norm(a),y=norm(b);return !!x&&!!y&&(x===y||x.includes(y)||y.includes(x));};
const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
const money=v=>num(v)==null?'Non déterminé':Math.round(num(v)).toLocaleString('fr-FR')+' €';
const med=a=>{const x=a.filter(Number.isFinite).sort((p,q)=>p-q);if(!x.length)return null;const i=Math.floor(x.length/2);return x.length%2?x[i]:(x[i-1]+x[i])/2;};
const pct=(a,p)=>{const x=a.filter(Number.isFinite).sort((m,n)=>m-n);return x.length?x[Math.round((x.length-1)*p)]:null;};
async function timedFetch(url,opt,ms){const c=new AbortController(),t=setTimeout(()=>c.abort(),ms);try{return await fetch(url,{...opt,signal:c.signal});}finally{clearTimeout(t);}}

const PROMPT='Analyse ces photos/captures de UNE MEME annonce automobile pour Vaut le Coup ?. Recoupe toutes les images. Lis uniquement ce qui est réellement visible et n’invente rien. Information absente ou illisible = null. Si des images se contredisent, signale précisément la contradiction dans warnings. Extrais marque, modèle, version/finition, année, kilométrage, prix, énergie, boîte, puissance, vendeur et lieu. Relève aussi entretien, distribution/courroie, contrôle technique, historique, accident, garantie, travaux, nombre de places et incohérences. Retourne UNIQUEMENT un objet JSON valide avec exactement ces clés : make, model, version, year, mileage_km, price_eur, energy, gearbox, power_hp, seller_type, location, title, confidence, uncertain_fields, visible_claims, warnings. confidence est un nombre de 0 à 100. seller_type vaut professional, private ou null. uncertain_fields, visible_claims et warnings sont des tableaux de chaînes.';

async function gemini(model,files){
  const body={contents:[{role:'user',parts:[{text:PROMPT},...files.map(f=>({inline_data:{mime_type:f.mimetype,data:f.buffer.toString('base64')}}))]}],generationConfig:{temperature:0,responseMimeType:'application/json',maxOutputTokens:1800}};
  const url='https://generativelanguage.googleapis.com/v1beta/models/'+encodeURIComponent(model)+':generateContent?key='+encodeURIComponent(GEMINI_KEY);
  let r;try{r=await timedFetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)},25000);}catch(e){const x=new Error(e?.name==='AbortError'?'TIMEOUT':'NETWORK');x.status=0;throw x;}
  const raw=await r.text();if(!r.ok){const e=new Error('HTTP_'+r.status);e.status=r.status;e.raw=raw.slice(0,500);throw e;}
  let d;try{d=JSON.parse(raw);}catch{throw new Error('BAD_RESPONSE');}
  const text=(d?.candidates?.[0]?.content?.parts||[]).map(p=>p.text||'').join('').trim();if(!text)throw new Error('EMPTY_RESPONSE');
  try{return JSON.parse(text);}catch{const a=text.indexOf('{'),b=text.lastIndexOf('}');if(a<0||b<=a)throw new Error('BAD_JSON');return JSON.parse(text.slice(a,b+1));}
}
async function analyze(files){if(!GEMINI_KEY)throw new Error('NO_GEMINI_KEY');let last;for(const model of MODELS){for(let i=0;i<2;i++){try{const v=await gemini(model,files);v.confidence=Math.max(0,Math.min(100,num(v.confidence)??0));for(const k of ['uncertain_fields','visible_claims','warnings'])if(!Array.isArray(v[k]))v[k]=[];v.model_used=model;return v;}catch(e){last=e;if(![429,500,502,503,504].includes(e.status)||i===1)break;await new Promise(r=>setTimeout(r,800));}}}throw last||new Error('GEMINI_FAILED');}

function comparable(x,v){if(!same(x.make,v.make)||!same(x.model,v.model))return false;const y=num(v.year),iy=num(x.year),k=num(v.mileage_km),ik=num(x.mileage);if(y!=null&&iy!=null&&Math.abs(y-iy)>4)return false;if(k!=null&&ik!=null&&Math.abs(k-ik)>60000)return false;if(v.energy&&x.energy&&!same(v.energy,x.energy))return false;if(v.gearbox&&x.gearbox&&!same(v.gearbox,x.gearbox))return false;return num(x.price)>0;}
async function market(v){if(!CARHUNT_KEY)return{ok:false,error:'Comparaison marché indisponible : clé CarHunt absente.'};if(!v?.make||!v?.model)return{ok:false,error:'Impossible de comparer : marque ou modèle non identifié.'};const q=new URLSearchParams({make:String(v.make).trim().toUpperCase(),model:String(v.model).trim().toUpperCase(),page_size:'100'});let r;try{r=await timedFetch('https://api-pro.carhunt.fr/v1/listings/search?'+q.toString(),{headers:{Authorization:'Bearer '+CARHUNT_KEY,Accept:'application/json'}},9000);}catch(e){return{ok:false,error:e?.name==='AbortError'?'Comparaison marché trop longue.':'CarHunt momentanément inaccessible.'};}const raw=await r.text();let d={};try{d=raw?JSON.parse(raw):{};}catch{}if(!r.ok){console.error('CarHunt',r.status,raw.slice(0,500));return{ok:false,error:'CarHunt a refusé la recherche (HTTP '+r.status+').'};}const comps=(Array.isArray(d.listings)?d.listings:[]).filter(x=>comparable(x,v)).map(x=>({...x,p:num(x.price)})).filter(x=>x.p>0),prices=comps.map(x=>x.p),m=med(prices),ask=num(v.price_eur);if(m==null)return{ok:true,comparables:0,asking:money(ask),median:null,score:null,label:'Marché insuffisant',sample:[]};const gap=ask==null?null:(m-ask)/m*100,score=gap==null?null:Math.max(0,Math.min(100,Math.round(50+gap*2.5)));const label=score==null?'Marché comparable':score>=80?'🔥 Très bonne affaire':score>=65?'👍 Prix très intéressant':score>=55?'🟢 Plutôt intéressant':score>=45?'🟡 Dans le marché':score>=35?'🟠 Plutôt cher':'🔴 Cher';return{ok:true,comparables:prices.length,asking:money(ask),median:Math.round(m),low:Math.round(pct(prices,.15)),high:Math.round(pct(prices,.85)),score,label,gap_pct:gap==null?null:Math.round(gap*10)/10,gap_eur:ask==null?null:Math.round(m-ask),warning:prices.length<8?'Échantillon limité : prudence dans le verdict.':null,sample:comps.slice(0,8).map(x=>({price:x.p,year:x.year,mileage:x.mileage,energy:x.energy,gearbox:x.gearbox,version:x.version,city:x.city,url:x.source_url}))};}

app.get('/api/health',(_req,res)=>res.json({ok:true,version:VERSION,provider:'gemini',models:MODELS,gemini:Boolean(GEMINI_KEY),carhunt:Boolean(CARHUNT_KEY)}));
app.post('/api/analyze',(req,res)=>{console.log('POST /api/analyze');upload.array('photos',3)(req,res,async err=>{if(err){console.error('upload',err);return res.status(400).json({ok:false,error:err.code==='LIMIT_FILE_SIZE'?'Une photo dépasse 12 Mo.':'Impossible de recevoir les photos.'});}try{const files=req.files||[];if(!files.length)return res.status(400).json({ok:false,error:'Ajoute au moins une photo.'});console.log('received',files.length,'photos');const vehicle=await analyze(files);console.log('Analyze OK',vehicle.make,vehicle.model);res.json({ok:true,vehicle,photos:files.length});}catch(e){console.error('Analyze error',e?.message,e?.status||'',e?.raw||'');let msg='Analyse IA indisponible. Réessaie.';if(e?.message==='NO_GEMINI_KEY')msg='Gemini n’est pas configuré sur le serveur.';else if(e?.message==='TIMEOUT')msg='Analyse trop longue. Réessaie.';else if(e?.status===429)msg='Gemini est momentanément très sollicité. Réessaie dans quelques secondes.';res.status(503).json({ok:false,error:msg});}});});
app.post('/api/market',async(req,res)=>{console.log('POST /api/market');try{const x=await market(req.body||{});res.status(x.ok?200:503).json(x);}catch(e){console.error('market',e);res.status(503).json({ok:false,error:'Comparaison marché indisponible.'});}});

const HTML=`<!doctype html><html lang="fr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#17212b"><title>Vaut le Coup ?</title><style>*{box-sizing:border-box}body{margin:0;background:#f4f6f8;color:#17212b;font-family:system-ui,-apple-system,Segoe UI,Roboto,sans-serif}.wrap{max-width:760px;margin:auto;padding:18px 12px 60px}.card{background:#fff;border:1px solid #e0e4e8;border-radius:22px;padding:20px;margin:14px 0;box-shadow:0 2px 10px #00000008}h1{font-size:36px;line-height:1.05;margin:0}h2{font-size:27px;margin:0 0 12px}.muted{color:#68737d}.picker{border:3px dashed #c8ced4;border-radius:20px;padding:24px 12px;text-align:center;min-height:150px;display:flex;flex-direction:column;justify-content:center;align-items:center}.pickbtn{border:0;background:#17212b;color:#fff;border-radius:14px;padding:14px 20px;font-size:18px;font-weight:850;cursor:pointer}.picker input{position:absolute;width:1px;height:1px;opacity:0}.small{display:block;color:#7b858e;margin-top:9px}.previews{display:grid;grid-template-columns:repeat(3,1fr);gap:9px;margin-top:12px}.thumb{position:relative;aspect-ratio:1}.thumb img{width:100%;height:100%;object-fit:cover;border-radius:14px;border:1px solid #ddd}.remove{position:absolute;right:-5px;top:-5px;width:34px;height:34px;border:0;border-radius:50%;background:#17212b;color:#fff;font-size:22px}.btn{width:100%;border:0;border-radius:15px;padding:17px;background:#17212b;color:#fff;font-size:19px;font-weight:850;margin-top:15px;min-height:56px}.btn:disabled{opacity:.45}.status{display:none;margin-top:14px;padding:15px;border-radius:17px;background:#eef2f5}.status.on{display:block}.track{height:10px;background:#dce2e7;border-radius:99px;overflow:hidden;margin-top:9px}.bar{height:100%;width:0;background:#2563eb;transition:width .2s}.grid{display:grid;grid-template-columns:1fr 1fr;gap:9px}.field{background:#f6f7f8;border-radius:13px;padding:11px}.field b{font-size:12px;color:#707b84}.pill{display:inline-block;background:#e9edf1;border-radius:99px;padding:6px 9px;margin:3px}.warn{background:#fff0d2;border-radius:15px;padding:13px;margin:9px 0}.result{padding:16px;border-radius:18px;background:#eef2f5}.score{font-size:50px;font-weight:950}.comp{padding:11px 0;border-bottom:1px solid #e1e5e8}.price{font-weight:900}.link{color:#174c7a;font-weight:700;text-decoration:none}@media(max-width:520px){h1{font-size:34px}.grid{grid-template-columns:1fr}.card{padding:18px}}</style></head><body><main class="wrap"><h1>Vaut le Coup ? ✓</h1><p class="muted">Avant d’acheter. Demande à l’IA.</p><section class="card"><h2>Analyse une annonce</h2><p>Ajoute jusqu’à 3 captures/photos de la même annonce.</p><div class="picker"><button id="pick" class="pickbtn" type="button">📸 Ajouter une photo</button><input id="photos" type="file" accept="image/jpeg,image/png,image/webp" multiple><span class="small">JPG, PNG ou WEBP — 12 Mo max par photo — 3 photos maximum</span></div><div id="previews" class="previews"></div><div id="status" class="status"><b id="statusTitle"></b><div class="track"><div id="bar" class="bar"></div></div><div id="statusText" class="muted" style="margin-top:8px"></div></div><button id="analyze" class="btn" type="button" disabled>Analyser l’annonce</button></section><section id="analysis" class="card" hidden></section><section id="market" class="card" hidden></section></main><script>
(function(){'use strict';var input=document.getElementById('photos'),pick=document.getElementById('pick'),previews=document.getElementById('previews'),btn=document.getElementById('analyze'),status=document.getElementById('status'),statusTitle=document.getElementById('statusTitle'),statusText=document.getElementById('statusText'),bar=document.getElementById('bar'),analysis=document.getElementById('analysis'),market=document.getElementById('market'),files=[],busy=false;
function esc(v){return String(v==null?'':v).replace(/[&<>\"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c];});}function showStatus(p,t,m){status.classList.add('on');bar.style.width=p+'%';statusTitle.textContent=t;statusText.textContent=m||'';}function render(){previews.innerHTML='';files.forEach(function(f,i){var d=document.createElement('div'),im=document.createElement('img'),x=document.createElement('button');d.className='thumb';im.src=URL.createObjectURL(f);im.alt='Photo '+(i+1);x.className='remove';x.type='button';x.textContent='×';x.onclick=function(){files.splice(i,1);render();};d.appendChild(im);d.appendChild(x);previews.appendChild(d);});btn.disabled=!files.length||busy;}
pick.onclick=function(){input.click();};input.onchange=function(){Array.from(input.files||[]).forEach(function(f){if(files.length<3&&/^image\/(jpeg|png|webp)$/.test(f.type)&&f.size<=12*1024*1024)files.push(f);});input.value='';render();};
function field(k,v){return '<div class="field"><b>'+esc(k)+'</b><br>'+esc(v==null||v===''?'Non déterminé':v)+'</div>';}
function showAnalysis(v){analysis.hidden=false;var h='<h2>Ce que l’IA a lu</h2><div class="grid">';h+=field('Marque',v.make)+field('Modèle',v.model)+field('Version / finition',v.version)+field('Année',v.year)+field('Kilométrage',v.mileage_km==null?null:Number(v.mileage_km).toLocaleString('fr-FR')+' km')+field('Prix',v.price_eur==null?null:Number(v.price_eur).toLocaleString('fr-FR')+' €')+field('Énergie',v.energy)+field('Boîte',v.gearbox)+field('Puissance',v.power_hp==null?null:String(v.power_hp)+' ch')+field('Vendeur',v.seller_type)+field('Lieu',v.location)+field('Confiance',String(v.confidence||0)+' / 100')+'</div>';if((v.visible_claims||[]).length)h+='<h3>Éléments visibles</h3>'+v.visible_claims.map(function(x){return '<span class="pill">'+esc(x)+'</span>';}).join('');if((v.uncertain_fields||[]).length)h+='<div class="warn"><b>À vérifier :</b><br>'+v.uncertain_fields.map(esc).join('<br>')+'</div>';if((v.warnings||[]).length)h+='<div class="warn"><b>Points d’attention :</b><br>'+v.warnings.map(esc).join('<br>')+'</div>';analysis.innerHTML=h;}
function showMarket(m){market.hidden=false;if(!m||!m.ok){market.innerHTML='<h2>Comparaison marché</h2><div class="warn">'+esc(m&&m.error?m.error:'Comparaison indisponible.')+'</div>';return;}var h='<h2>Comparaison marché</h2><div class="result"><div class="score">'+esc(m.score==null?'—':m.score+'/100')+'</div><h3>'+esc(m.label||'')+'</h3><p>Médiane du marché : <b>'+esc(m.median==null?'—':Number(m.median).toLocaleString('fr-FR')+' €')+'</b></p><p>Fourchette indicative : <b>'+esc(m.low==null?'—':Number(m.low).toLocaleString('fr-FR')+' €')+'</b> à <b>'+esc(m.high==null?'—':Number(m.high).toLocaleString('fr-FR')+' €')+'</b></p><p>Écart : <b>'+esc(m.gap_eur==null?'—':Number(m.gap_eur).toLocaleString('fr-FR')+' €')+'</b> ('+esc(m.gap_pct==null?'—':m.gap_pct+' %')+')</p></div>';if(m.warning)h+='<div class="warn">'+esc(m.warning)+'</div>';h+='<h3>Véhicules comparables ('+esc(m.comparables)+')</h3>';(m.sample||[]).forEach(function(x){h+='<div class="comp"><span class="price">'+esc(Number(x.price).toLocaleString('fr-FR')+' €')+'</span> — '+esc(x.year||'')+' — '+esc(x.mileage?Number(x.mileage).toLocaleString('fr-FR')+' km':'')+(x.city?' — '+esc(x.city):'')+(x.url?' — <a class="link" target="_blank" rel="noopener" href="'+esc(x.url)+'">Voir l’annonce</a>':'')+'</div>';});market.innerHTML=h;}
function compress(f){return new Promise(function(resolve,reject){var im=new Image(),u=URL.createObjectURL(f);im.onload=function(){var max=1800,w=im.naturalWidth,h=im.naturalHeight;if(w>max||h>max){if(w>=h){h=Math.round(h*max/w);w=max;}else{w=Math.round(w*max/h);h=max;w=max;}}var c=document.createElement('canvas');c.width=w;c.height=h;c.getContext('2d').drawImage(im,0,0,w,h);URL.revokeObjectURL(u);c.toBlob(function(b){b?resolve(new File([b],'photo.jpg',{type:'image/jpeg'})):reject(new Error('compression'));},'image/jpeg',.82);};im.onerror=function(){URL.revokeObjectURL(u);reject(new Error('image'));};im.src=u;});}
async function run(){if(busy||!files.length)return;busy=true;btn.disabled=true;analysis.hidden=true;market.hidden=true;try{showStatus(10,'Préparation','Optimisation des photos…');var optimized=[];for(var i=0;i<files.length;i++){showStatus(10+i*8,'Préparation','Photo '+(i+1)+'/'+files.length+'…');optimized.push(await compress(files[i]));}var fd=new FormData();optimized.forEach(function(f){fd.append('photos',f,f.name);});showStatus(30,'Analyse IA','Lecture de l’annonce…');var ctl=new AbortController(),timer=setTimeout(function(){ctl.abort();},70000),r;try{r=await fetch('/api/analyze?client='+Date.now(),{method:'POST',body:fd,cache:'no-store',credentials:'same-origin',signal:ctl.signal,headers:{'X-VLC-Version':VERSION}});}finally{clearTimeout(timer);}var t=await r.text(),d;try{d=JSON.parse(t);}catch{throw new Error('Réponse serveur invalide ('+r.status+').');}if(!r.ok||!d.ok)throw new Error(d.error||'Analyse impossible.');showStatus(65,'Analyse terminée','Véhicule identifié.');showAnalysis(d.vehicle);showStatus(72,'Marché','Recherche des comparables…');var mr=await fetch('/api/market?client='+Date.now(),{method:'POST',headers:{'Content-Type':'application/json','X-VLC-Version':VERSION},body:JSON.stringify(d.vehicle),cache:'no-store',credentials:'same-origin'}),mt=await mr.text(),md;try{md=JSON.parse(mt);}catch{md={ok:false,error:'Réponse marché invalide.'};}showMarket(md);showStatus(100,'Terminé','Analyse et comparaison disponibles.');}catch(e){showStatus(100,'Impossible de terminer',e&&e.name==='AbortError'?'La connexion a pris trop de temps.':(e&&e.message?e.message:'Échec de la connexion au serveur.'));}finally{busy=false;btn.disabled=!files.length;}}
btn.onclick=run;
})();</script></body></html>`;

app.get('/',(_req,res)=>res.type('html').send(HTML));
app.listen(PORT,()=>console.log('Vaut le Coup ? — '+VERSION+' — '+PORT));